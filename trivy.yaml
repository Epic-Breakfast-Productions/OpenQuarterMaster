format: sarif
report: "all"
version: 
severity: 
  - UNKNOWN
  - LOW
  - MEDIUM
  - HIGH
  - CRITICAL
db:
  skip-update: true
scanners: 'vuln,secret,misconfig,license'
output: 'trivy-results.sarif'
skip-setup-trivy: true