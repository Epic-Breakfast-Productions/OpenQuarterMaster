
var unitOptions = $("#unitSelectOptions").children();

function getUnitOptions(selectedVal){
    var output = unitOptions.clone();

    if(selectedVal){
        //TODO:: select option selected
    }
    return output;
}