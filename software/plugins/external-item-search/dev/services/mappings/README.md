# Service Mappings

The json documents in this directory outline the responses given from the various data providers we support. They are wiremock stibs that simulate that bhavior.

## Conventions

 - API Keys
   - `devKey` is always used as a valid API key, if applicable
   - `devKeyBad` is always used as an INvalid API key, in turn


