greeting:
  message: "hello"
mp:
  messaging:
    outgoing:
      words-out:
        topic: "words"
    incoming:
      words-in:
        auto:
          offset:
            reset: "earliest"
        topic: "words"

quarkus:
  
  keycloak:
    devservices:
      port: 9328
      realm-path: "dev/oqm-realm.json"
      realm-name: oqm

"%dev":
  quarkus:
    http:
      port: 8080
      ssl-port: 8443
    oidc:
      # TODO:: remove when https://github.com/quarkusio/quarkus/issues/47581
      client-id: oqm-app
      credentials:
        secret: "**********"
